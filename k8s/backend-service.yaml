apiVersion: v1
kind: Service
metadata:
  name: campus-beats-backend
spec:
  selector:
    app: campus-beats-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
